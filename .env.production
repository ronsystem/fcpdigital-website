# FCP Digital Website - Production Environment Variables
# Copy from RonOS .env and Stripe/Supabase dashboards
# DO NOT commit this file to Git

# ============ SUPABASE (Database & Auth) ============
NEXT_PUBLIC_SUPABASE_URL=https://obkqnqlnyszswrkbohny.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<YOUR_SUPABASE_ANON_KEY>
SUPABASE_SERVICE_ROLE_KEY=<YOUR_SUPABASE_SERVICE_ROLE_KEY>

# ============ STRIPE (Payments) ============
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_<YOUR_STRIPE_PUBLISHABLE_KEY>
STRIPE_SECRET_KEY=sk_live_<YOUR_STRIPE_SECRET_KEY>
STRIPE_WEBHOOK_SECRET=whsec_<YOUR_STRIPE_WEBHOOK_SECRET>

# ============ RONOS BACKEND (Provisioning Webhook) ============
RONOS_WEBHOOK_URL=https://<your-ronos-server>.com/webhook/stripe

# ============ APP CONFIGURATION ============
NEXT_PUBLIC_APP_URL=https://fcpdigital.com

# ============ HOW TO GET THESE VALUES ============
#
# NEXT_PUBLIC_SUPABASE_ANON_KEY:
#   → Supabase Dashboard (https://app.supabase.com)
#   → Select project
#   → Settings → API
#   → Copy "anon" public key
#
# SUPABASE_SERVICE_ROLE_KEY:
#   → From RonOS .env file at /Users/apple/Desktop/ronos/.env
#   → Look for: SUPABASE_SERVICE_ROLE_KEY=eyJh...
#
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY:
#   → Stripe Dashboard (https://dashboard.stripe.com)
#   → Developers → API Keys
#   → Copy "Publishable key" (starts with pk_live_)
#
# STRIPE_SECRET_KEY:
#   → Stripe Dashboard → Developers → API Keys
#   → Copy "Secret key" (starts with sk_live_)
#   → Or get from RonOS .env
#
# STRIPE_WEBHOOK_SECRET:
#   → Stripe Dashboard → Developers → Webhooks
#   → Create webhook endpoint: https://fcpdigital.com/api/webhooks/stripe
#   → Events: customer.subscription.created, .updated, .deleted, invoice.payment_failed
#   → Click endpoint → Reveal "Signing secret" (starts with whsec_)
#
# RONOS_WEBHOOK_URL:
#   → Your RonOS Flask server public URL
#   → If using ngrok: https://xxx-xxx-xxx-xxx.ngrok.io/webhook/stripe
#   → If using AWS: https://ronos.yourserver.com/webhook/stripe
#   → Must be accessible from the internet
#
# NEXT_PUBLIC_APP_URL:
#   → Your production domain: https://fcpdigital.com
#   → Or Vercel preview domain: https://fcpdigital-website.vercel.app
